import React from 'react';
import style from '../home.style';

const s = Object.create(style);

s.wrapper = {
	margin: '0 auto',
	textAlign: 'left',
	maxWidth: '300px'
};

s.types = {
	float: 'left'
};

s.tribes = {
	float: 'right'
};

export default s;
